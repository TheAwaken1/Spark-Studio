# Spark Studio — agent instructions

- At the end of every working session, append an entry to `SESSIONS.md`
  (newest at the top) using the format described at the top of that file.
  Include what was asked, what changed, and any decisions/gotchas.
